const cutDialog = require('./cutDialog');
const systemDialog = require('./systemDialog');
console.log('------->><<MMMMMM', cutDialog, systemDialog)
module.exports = {
    cutDialog,
    systemDialog
}